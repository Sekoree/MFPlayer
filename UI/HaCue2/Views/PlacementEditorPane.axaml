<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="using:HaCue2.Controls"
             xmlns:vm="using:HaCue2.ViewModels"
             x:Class="HaCue2.Views.PlacementEditorPane"
             x:DataType="vm:InspectorViewModel"
             x:CompileBindings="True"
             AutomationProperties.Name="Video placement editor">

  <!-- One placement's complete editor. InspectorPane places this inside each placement expander;
       all instances bind to the inspector's selected placement, and only the open one is realised. -->
  <StackPanel Margin="0,4,0,2">
    <HeaderedContentControl Theme="{StaticResource Field}" Header="Composition">
      <ComboBox ItemsSource="{Binding PlacementCompositions}"
                SelectedIndex="{Binding PlacementCompositionIndex}" />
    </HeaderedContentControl>
    <HeaderedContentControl Theme="{StaticResource Field}" Header="Layer">
      <TextBox Classes="short" Text="{Binding LayerValue}" LostFocus="OnFieldCommitted" />
    </HeaderedContentControl>
    <HeaderedContentControl Theme="{StaticResource Field}" Header="Fit">
      <ComboBox ItemsSource="{Binding FitModes}" SelectedIndex="{Binding FitIndex}" />
    </HeaderedContentControl>
    <HeaderedContentControl Theme="{StaticResource Field}" Header="Opacity">
      <NumericUpDown Minimum="0" Maximum="1" Increment="0.05" FormatString="0.##"
                     Value="{Binding PlacementOpacity}" />
    </HeaderedContentControl>

    <!-- The destination rectangle as NUMBERS. A drag cannot hit exactly half, and half is what a
         split screen is made of. -->
    <HeaderedContentControl Theme="{StaticResource FieldTight}" Header="Dest x / y">
      <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
        <NumericUpDown Grid.Column="0" Minimum="-1" Maximum="2" Increment="0.01"
                       FormatString="0.###" Value="{Binding PlacementX}" />
        <NumericUpDown Grid.Column="1" Minimum="-1" Maximum="2" Increment="0.01"
                       FormatString="0.###" Value="{Binding PlacementY}" />
      </Grid>
    </HeaderedContentControl>
    <HeaderedContentControl Theme="{StaticResource FieldTight}" Header="Dest w / h">
      <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
        <NumericUpDown Grid.Column="0" Minimum="0.001" Maximum="2" Increment="0.01"
                       FormatString="0.###" Value="{Binding PlacementWidth}" />
        <NumericUpDown Grid.Column="1" Minimum="0.001" Maximum="2" Increment="0.01"
                       FormatString="0.###" Value="{Binding PlacementHeight}" />
      </Grid>
    </HeaderedContentControl>
    <HeaderedContentControl Theme="{StaticResource Field}" Header="Rotation">
      <NumericUpDown Minimum="-360" Maximum="360" Increment="1" FormatString="0.##"
                     Value="{Binding PlacementRotation}"
                     ToolTip.Tip="Clockwise, about the rectangle's centre. A rotated layer overflows its rectangle rather than being trimmed to it." />
    </HeaderedContentControl>

    <ContentControl Theme="{StaticResource FieldWide}">
      <WrapPanel ItemSpacing="4" LineSpacing="4">
        <Button Classes="tight" Content="FULL" Click="OnLayout" Tag="full" AutomationProperties.Name="Place full frame" />
        <Button Classes="tight" Content="◧" Click="OnLayout" Tag="left" ToolTip.Tip="Left half" AutomationProperties.Name="Place in left half" />
        <Button Classes="tight" Content="◨" Click="OnLayout" Tag="right" ToolTip.Tip="Right half" AutomationProperties.Name="Place in right half" />
        <Button Classes="tight" Content="⬒" Click="OnLayout" Tag="top" ToolTip.Tip="Top half" AutomationProperties.Name="Place in top half" />
        <Button Classes="tight" Content="⬓" Click="OnLayout" Tag="bottom" ToolTip.Tip="Bottom half" AutomationProperties.Name="Place in bottom half" />
        <Button Classes="tight" Content="◰" Click="OnLayout" Tag="tl" ToolTip.Tip="Top left" AutomationProperties.Name="Place in top left" />
        <Button Classes="tight" Content="◳" Click="OnLayout" Tag="tr" ToolTip.Tip="Top right" AutomationProperties.Name="Place in top right" />
        <Button Classes="tight" Content="◱" Click="OnLayout" Tag="bl" ToolTip.Tip="Bottom left" AutomationProperties.Name="Place in bottom left" />
        <Button Classes="tight" Content="◲" Click="OnLayout" Tag="br" ToolTip.Tip="Bottom right" AutomationProperties.Name="Place in bottom right" />
      </WrapPanel>
    </ContentControl>

    <HeaderedContentControl Theme="{StaticResource Field}" Header="Resize">
      <WrapPanel ItemSpacing="12" LineSpacing="4">
        <CheckBox Content="preserve aspect" IsChecked="{Binding PreservePlacementAspect}" />
        <CheckBox Content="snap + guides" IsChecked="{Binding SnapPlacement}" />
      </WrapPanel>
    </HeaderedContentControl>

    <!-- Source crop is which part of the picture to use, not where to put it. -->
    <ContentControl Theme="{StaticResource Subhead}" Content="Source crop" />
    <HeaderedContentControl Theme="{StaticResource FieldTight}" Header="Left / top">
      <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
        <NumericUpDown Grid.Column="0" Minimum="0" Maximum="0.49" Increment="0.01"
                       FormatString="0.###" Value="{Binding CropLeft}" />
        <NumericUpDown Grid.Column="1" Minimum="0" Maximum="0.49" Increment="0.01"
                       FormatString="0.###" Value="{Binding CropTop}" />
      </Grid>
    </HeaderedContentControl>
    <HeaderedContentControl Theme="{StaticResource FieldTight}" Header="Right / bottom">
      <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
        <NumericUpDown Grid.Column="0" Minimum="0" Maximum="0.49" Increment="0.01"
                       FormatString="0.###" Value="{Binding CropRight}" />
        <NumericUpDown Grid.Column="1" Minimum="0" Maximum="0.49" Increment="0.01"
                       FormatString="0.###" Value="{Binding CropBottom}" />
      </Grid>
    </HeaderedContentControl>
    <ContentControl Theme="{StaticResource FieldWide}">
      <WrapPanel ItemSpacing="4" LineSpacing="4">
        <Button Classes="tight" Content="NO CROP" Click="OnCrop" Tag="none" />
        <Button Classes="tight" Content="CENTRE ½ WIDE" Click="OnCrop" Tag="wide" />
        <Button Classes="tight" Content="CENTRE ½ TALL" Click="OnCrop" Tag="tall" />
        <Button Classes="tight" Content="CENTRE BOX" Click="OnCrop" Tag="centre" />
      </WrapPanel>
    </ContentControl>

    <!-- Ordered rack: identity and order live here; focused built-in editors follow below. -->
    <ContentControl Theme="{StaticResource Subhead}" Content="Effect rack" />
    <ItemsControl ItemsSource="{Binding EffectRackRows}">
      <ItemsControl.ItemTemplate>
        <DataTemplate x:DataType="vm:LayerEffectRackRow">
          <Grid Margin="10,2" ColumnDefinitions="Auto,*,Auto,Auto,Auto,Auto" ColumnSpacing="4">
            <TextBlock Grid.Column="0" Classes="mono dim3" VerticalAlignment="Center"
                       Text="{Binding Number, StringFormat='#{0}'}" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
              <TextBlock Text="{Binding Name}" />
              <TextBlock Classes="small dim3" Text="{Binding Enabled, StringFormat='enabled: {0}'}" />
            </StackPanel>
            <Button Grid.Column="2" Classes="tight" Content="ON/OFF" Tag="{Binding Id}"
                    Click="OnToggleEffect" />
            <Button Grid.Column="3" Classes="tight" Content="↑" AutomationProperties.Name="Move effect earlier" Tag="{Binding Id}"
                    IsEnabled="{Binding CanMoveUp}" Click="OnMoveEffectUp" />
            <Button Grid.Column="4" Classes="tight" Content="↓" AutomationProperties.Name="Move effect later" Tag="{Binding Id}"
                    IsEnabled="{Binding CanMoveDown}" Click="OnMoveEffectDown" />
            <Button Grid.Column="5" Classes="tight" Content="×" AutomationProperties.Name="Remove effect" Tag="{Binding Id}"
                    Click="OnRemoveEffect" />
          </Grid>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <Button Classes="block" Content="ADD EFFECT ▾">
      <Button.Flyout>
        <MenuFlyout>
          <MenuItem Header="Chroma key" Tag="chroma-key.v1" Click="OnAddEffect" />
          <MenuItem Header="Brightness / contrast" Tag="brightness-contrast.v1" Click="OnAddEffect" />
        </MenuFlyout>
      </Button.Flyout>
    </Button>

    <!-- OFF IS NOT DELETE: effect settings survive their toggles. -->
    <ContentControl Theme="{StaticResource Subhead}" Content="Chroma key" />
    <HeaderedContentControl Theme="{StaticResource Field}" Header="Key">
      <CheckBox IsChecked="{Binding ChromaKeyOn}" Content="key this layer" />
    </HeaderedContentControl>
    <StackPanel IsVisible="{Binding HasChromaKey}">
      <HeaderedContentControl Theme="{StaticResource Field}" Header="Colour">
        <TextBox Text="{Binding ChromaColour}" PlaceholderText="#00FF00" />
      </HeaderedContentControl>
      <HeaderedContentControl Theme="{StaticResource FieldTight}" Header="Similar / soft">
        <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
          <NumericUpDown Grid.Column="0" Minimum="0" Maximum="1" Increment="0.02"
                         FormatString="0.##" Value="{Binding ChromaSimilarity}" />
          <NumericUpDown Grid.Column="1" Minimum="0" Maximum="1" Increment="0.02"
                         FormatString="0.##" Value="{Binding ChromaSmoothness}" />
        </Grid>
      </HeaderedContentControl>
      <HeaderedContentControl Theme="{StaticResource Field}" Header="Spill">
        <NumericUpDown Minimum="0" Maximum="1" Increment="0.02" FormatString="0.##"
                       Value="{Binding ChromaSpill}"
                       ToolTip.Tip="How much of the key colour is pulled out of what remains — the green fringe." />
      </HeaderedContentControl>
      <Button Classes="block" Content="AUTOMATE CHROMA ▾">
        <Button.Flyout>
          <MenuFlyout>
            <MenuItem Header="Similarity" Click="OnAddAutomation" Tag="video.effect.chroma-key.similarity"
                      IsEnabled="{Binding CanAddChromaSimilarityLane}" />
            <MenuItem Header="Smoothness" Click="OnAddAutomation" Tag="video.effect.chroma-key.smoothness"
                      IsEnabled="{Binding CanAddChromaSmoothnessLane}" />
            <MenuItem Header="Spill reduction" Click="OnAddAutomation" Tag="video.effect.chroma-key.spill-reduction"
                      IsEnabled="{Binding CanAddChromaSpillLane}" />
          </MenuFlyout>
        </Button.Flyout>
      </Button>
    </StackPanel>

    <ContentControl Theme="{StaticResource Subhead}" Content="Colour" />
    <HeaderedContentControl Theme="{StaticResource Field}" Header="Adjust">
      <CheckBox IsChecked="{Binding ColorAdjustOn}" Content="brightness and contrast" />
    </HeaderedContentControl>
    <HeaderedContentControl Theme="{StaticResource FieldTight}" Header="Bright / contrast"
                            IsVisible="{Binding ColorAdjustOn}">
      <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
        <NumericUpDown Grid.Column="0" Minimum="-1" Maximum="1" Increment="0.05"
                       FormatString="0.##" Value="{Binding LayerBrightness}" />
        <NumericUpDown Grid.Column="1" Minimum="0" Maximum="4" Increment="0.05"
                       FormatString="0.##" Value="{Binding LayerContrast}" />
      </Grid>
    </HeaderedContentControl>
    <Button Classes="block" Content="AUTOMATE COLOUR ▾" IsVisible="{Binding HasColorAdjust}">
      <Button.Flyout>
        <MenuFlyout>
          <MenuItem Header="Brightness" Click="OnAddAutomation" Tag="video.effect.color-adjust.brightness"
                    IsEnabled="{Binding CanAddColorBrightnessLane}" />
          <MenuItem Header="Contrast" Click="OnAddAutomation" Tag="video.effect.color-adjust.contrast"
                    IsEnabled="{Binding CanAddColorContrastLane}" />
        </MenuFlyout>
      </Button.Flyout>
    </Button>

    <!-- The guides are seams between the screens dividing this placement's canvas. -->
    <c:AspectBox Margin="10,6" VerticalAlignment="Top" Ratio="{Binding PlacementAspect}">
      <c:PlacementCanvas Boxes="{Binding Placements}"
                         AutomationProperties.Name="Video placement canvas"
                         AutomationProperties.HelpText="Drag or resize this placement. Exact numeric fields are above."
                         GuidesX="{Binding PlacementGuidesX}"
                         GuidesY="{Binding PlacementGuidesY}"
                         PreserveAspect="{Binding PreservePlacementAspect}"
                         SnapEnabled="{Binding SnapPlacement}"
                         AllowOutside="True"
                         Gesture="OnPlacementGesture"
                         GestureCompleted="OnPlacementGestureCompleted" />
    </c:AspectBox>

    <Border Classes="chain">
      <DockPanel LastChildFill="True">
        <Button DockPanel.Dock="Right" Classes="tight" Content="REMOVE" Click="OnRemovePlacement" />
        <Button DockPanel.Dock="Right" Margin="0,0,5,0" Classes="tight" Content="POP OUT ↗"
                Click="OnPopOut" ToolTip.Tip="Open a large placement canvas for precise mouse editing." />
        <Border DockPanel.Dock="Left" Margin="0,0,7,0" Classes="badge">
          <TextBlock Text="PLACEMENT" />
        </Border>
        <TextBlock Classes="mono dim3 small" VerticalAlignment="Center"
                   Text="drag here for rough · pop out for precision" />
      </DockPanel>
    </Border>
  </StackPanel>
</UserControl>
