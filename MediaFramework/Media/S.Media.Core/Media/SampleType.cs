namespace S.Media.Core.Media;

/// <summary>PCM sample representation used inside the pipeline. Float32 is the canonical internal format.</summary>
public enum SampleType
{
    Float32,
    Int16,
    Int24,
    Int32
}

